import { types } from '../constants'

export const clearEmployeeSuccess = () => {
    return {
        type: types.CLEAR_EMPLOYEE_SUCCESS
    }
}

export const clearDeleteSuccess = () => {
    return {
        type: types.CLEAR_DELETE_SUCCESS
    }
}